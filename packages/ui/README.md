# @genflow/ui

Shared UI component library for GenFlow platform.

- GenButton — Primary, outline, danger variants with loading state
- GenCard — Basic and elevated card containers
- GenInput — Form input with label
- GenBadge — Status badges (success, warning, error, info)
